{
  "schema": "neko.agent-eval.scenario.v2",
  "id": "external-perception-when-chat-differs",
  "suiteId": "agent-runtime.perception-routing",
  "caseGroup": "canonical",
  "visibility": "public",
  "evidenceContract": {
    "userBehavior": "Generate an image with a non-vision chat model and analyze the real result through the perception capability.",
    "canonicalPath": ["explicit non-vision chat model", "GenerateImage", "task result", "ReadImage resource identity", "perception.perceive"],
    "forbiddenFallback": ["guess from generation prompt", "native vision assumption", "unobserved task result"],
    "observables": [{ "ref": "perception-facts", "kind": "runtime-fact", "description": "Actual model, image task, perception Tool call, and final analysis facts.", "required": true }],
    "expectedResult": "The non-vision model routes the delivered image reference through perception.perceive before analysis.",
    "expectedFailure": "Model fallback, missing task result, absent perception call, or guessed output fails."
  },
  "fixtureRefs": ["empty-workspace"],
  "runtimeProfileId": "canonical-default",
  "modelProfileIds": ["deepseek-no-native-vision"],
  "steps": [
    { "id": "submit", "kind": "submit", "prompt": "生成一张猫玩红球的图片。任务完成后读取真实资源身份，调用 perception.perceive，再基于感知结果说明画面、构图、光影和质量问题；不要根据提示词猜测。" },
    { "id": "idle", "kind": "wait-for-idle", "timeoutMs": 600000 }
  ],
  "assertions": [
    { "id": "model", "kind": "model", "profileId": "deepseek-no-native-vision", "noFallback": true, "evidenceRef": "perception-facts" },
    { "id": "task", "kind": "task-terminal", "taskType": "image_generation", "status": "completed", "evidenceRef": "perception-facts" },
    { "id": "perception", "kind": "tool-call", "name": "perception.perceive", "status": "success", "evidenceRef": "perception-facts" },
    { "id": "answer", "kind": "final-answer", "mode": "contains", "text": ["猫", "画面"], "evidenceRef": "perception-facts" }
  ],
  "artifactChecks": [],
  "budget": { "timeoutMs": 600000, "repetitions": 1 }
}
