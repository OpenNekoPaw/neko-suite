{
  "schema": "neko.agent-eval.scenario.v2",
  "id": "native-vision-without-perception-tool",
  "suiteId": "agent-runtime.perception-routing",
  "caseGroup": "boundary",
  "visibility": "public",
  "evidenceContract": {
    "userBehavior": "Generate and analyze an image through matching native chat vision without the external perception Tool.",
    "canonicalPath": ["explicit native-vision chat model", "GenerateImage", "task result", "native multimodal context"],
    "forbiddenFallback": ["perception.perceive", "different chat model", "prompt-only analysis"],
    "observables": [{ "ref": "perception-facts", "kind": "runtime-fact", "description": "Actual model, task result, absence of external perception Tool, and final analysis.", "required": true }],
    "expectedResult": "Matching native vision analyzes the delivered image without perception.perceive.",
    "expectedFailure": "Model fallback, missing task result, external perception call, or empty analysis fails."
  },
  "fixtureRefs": ["empty-workspace"],
  "runtimeProfileId": "canonical-default",
  "modelProfileIds": ["native-vision"],
  "steps": [
    { "id": "submit", "kind": "submit", "prompt": "生成一张猫玩红球的图片。任务完成后使用原生图像上下文分析真实结果，不要调用 perception.perceive；说明画面、构图、光影和质量问题。" },
    { "id": "idle", "kind": "wait-for-idle", "timeoutMs": 600000 }
  ],
  "assertions": [
    { "id": "model", "kind": "model", "profileId": "native-vision", "noFallback": true, "evidenceRef": "perception-facts" },
    { "id": "task", "kind": "task-terminal", "taskType": "image_generation", "status": "completed", "evidenceRef": "perception-facts" },
    { "id": "no-external-perception", "kind": "tool-call", "name": "perception.perceive", "status": "absent", "evidenceRef": "perception-facts" },
    { "id": "answer", "kind": "final-answer", "mode": "contains", "text": ["猫", "画面"], "evidenceRef": "perception-facts" }
  ],
  "artifactChecks": [],
  "budget": { "timeoutMs": 600000, "repetitions": 1 }
}
