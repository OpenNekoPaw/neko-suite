# Agent functional fixture

This isolated workspace intentionally contains no provider credentials. The P0 UI scenario verifies
the real Extension Webview activation, input path, host projection, lifecycle, and explicit missing
configuration behavior without calling an external model.
