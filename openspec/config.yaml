schema: spec-driven

# Project context shown to AI when creating OpenSpec artifacts.
# Keep detailed project development principles in project.md so proposal/design/spec/task
# instructions all share one source of truth.
context: |
  Read and follow openspec/project.md before creating or updating proposal,
  design, specs, or tasks artifacts. It defines Neko Suite's architecture
  boundaries, contract-first workflow, ADR-derived development principles,
  risk levels, validation expectations, and domain-specific constraints.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
