## 1. Contracts and Inventory

- [x] 1.1 Inventory Neko-owned human strings and output sinks across TUI/CLI router, Ink components, hooks/ports, shared Agent handlers, startup/status chrome, one-shot commands, menus, diagnostics, Commander help/usage/parse failures and utility/experiment terminal notices. Inventory JSON, debug JSONL, structured-log, evaluation, completion-script and report sinks separately; classify each item by human or machine channel and as `@neko/agent`, existing `cli-tui` package ownership, stable token, external text or report-domain content. Keep this task hierarchy as the only owned-output inventory, organized by output family/sink rather than individual message key; do not create a separate inventory document, manifest, runtime registry or second release checklist.
  - Human / `@neko/cli` startup and direct process sinks: Commander name/description/options/arguments/help/usage/parse diagnostics, workdir/config/auth/bootstrap failures, interactive startup/shutdown notices, market and other one-shot command output, and stdout/stderr routing in `cli.tsx` and command adapters.
  - Human / `@neko/cli` command projection: slash catalog/menu descriptions and the router families for generic status, configuration, model, media, perception, MCP, capability diagnostics, artifacts, Skills, tasks, cost/context/session controls and expected typed diagnostics; `TuiCommandRouterResult.output`/`error` remain only the final rendered projection.
  - Human / `@neko/cli` Ink and hook projection: App chrome, status/token bars, activity/thinking/todo/timeline rows, queue/selection/reference/suggestion menus, approval/diff/command previews, error boundary and hook-generated session/reference/background-task diagnostics. ANSI, wrapping, indentation, truncation, icons and Ink styling are adapter concerns rather than bundle messages.
  - Human / `@neko/cli` utility and experiment terminal notices: terminal capability warnings, progress/spinner/status lines and interactive experiment notices emitted to a terminal; persisted experiment JSON/Markdown bodies are classified separately as machine/report-domain output.
  - Human / `@neko/agent`: reusable command handlers, lifecycle/hooks and domain read models that currently manufacture Neko-owned final prose consumed by the TUI. These migrate to `agent.command.*` semantic results/messages; the CLI must not duplicate their wording.
  - External text (preserved verbatim): user/model/assistant content, tool/provider/MCP/custom Skill names and descriptions, external provider errors, project/user-authored labels and file contents. Neko-owned surrounding labels and diagnostics are localized without translating the external payload.
  - Stable tokens (preserved and placeholder-injected): slash/Commander command names, option flags, environment/config keys, diagnostic/status codes, model/provider/tool/capability IDs, enum values, paths/URIs, hashes, timestamps, numeric values and protocol field names.
  - Machine / protocol sinks (locale-neutral): debug-automation JSONL reserves stdout for protocol frames; JSON one-shot/probe output, evaluation facts/results, completion scripts and structured log records keep byte/schema-stable keys and values. Bootstrap/programming failures go to stderr with non-zero exit and never become protocol stdout prose.
  - Report-domain sinks (not terminal bundles): experiment/evaluation generated JSON and Markdown report bodies, snapshots and persisted artifacts retain their owning schema/content rules; only contemporaneous terminal progress or completion notices are localized.
  - Verification inventory: tests and residual searches must cover direct `process.stdout`/`process.stderr`/console writes, router `output`/`error`, Ink literals, hook fallbacks, Commander metadata, `JSON.stringify`, debug stdio, completion/report generators, legacy `tui-locale` helpers and `NEKO_LOCALE`; no second manifest or runtime inventory is introduced.
- [x] 1.2 Define the canonical `AgentTerminalPresentationContext`, command-family-specific semantic results, typed terminal diagnostics, generic status projection contract, pure status Presenter, statically dispatched command-family Presenters and the retained outer `TuiCommandRouterResult` terminal projection contract before migrating concrete commands. Place the status contract in a dedicated package-private module inside the existing `cli-tui` package, separate from `tui-command-router.ts`, without a new subpackage or public-index export; compose owning-domain/runtime read models and required `userConfigPath` without parallel TUI domain DTOs or preformatted Neko-owned `*Summary` prose.
- [x] 1.3 Add failing baseline tests proving the model/media/perception family remains partially English under `zh-cn` and identifying any legacy path that can still return final prose.

## 2. Locale and Bundle Composition

- [x] 2.1 Add a small Layer-0 `createStrictTranslator`-style primitive to the existing `@neko/shared/i18n` entrypoint. It accepts only resolved `SupportedLocale`, statically supplied bundles and exact string/number parameters; validates bundle/key/placeholder parity, named placeholder syntax, `{{`/`}}` brace escapes and exact runtime parameter sets once during translator construction; and fails visibly for missing/extra/undefined parameters, invalid bundles, collisions and missing keys without English/key fallback. Keep it free of Node, React, environment, filesystem, discovery, registry and global-singleton concerns. Preserve existing Webview `I18nService` mutation/fallback behavior and do not use `setLocale()` in terminal composition.
- [x] 2.2 Colocate flat `agent.command.*` `en`/`zh-cn` bundles, key type and parity tests in `@neko/agent` and expose them through one narrow terminal-message module rather than the root barrel; colocate package-private `agent.terminal.*` bundles in `@neko/cli`. Statically import both into CLI composition without reverse dependency, filesystem/glob discovery, dynamic import, registry, plugin contribution, locale pack or lazy locale chunk. Implement complete independently translatable semantic messages with stable values injected through placeholders. Derive each owner key union from its canonical English bundle, compose the two unions for terminal translation and enforce exact `en`/`zh-cn` key plus placeholder-name parity using `Readonly<Record<string, string | number>>` parameters. Represent required English singular/plural distinctions with explicit parity-checked keys selected by the Presenter; do not add handwritten key enums, codegen, protobuf schemas, a central catalog, per-key parameter interfaces, ICU MessageFormat, Fluent or another plural/message DSL.
- [x] 2.3 Implement paired canonical sources `--ui-locale` / `NEKO_UI_LOCALE` / `ui_locale` and `--prompt-locale` / `NEKO_PROMPT_LOCALE` / `prompt_locale` with UI CLI → environment → user → host and Prompt CLI → environment → user → resolved UI precedence. Do not add generic `--locale`; delete `NEKO_LOCALE` without alias, warning adapter or UI/prompt fan-out.
- [x] 2.4 Make source adapters preserve absent versus present raw input without trim/lowercase/normalization/precedence/fallback, then validate every present explicit source centrally and reject workspace `ui_locale`/`prompt_locale`; cover empty, leading/trailing whitespace, alternate casing, unsupported alias/variant and shadowed-invalid cases. Implement host-only normalization so absence of explicit configuration follows the operating system's first preferred language, Chinese families map to `zh-cn`, and unsupported/unavailable values resolve to `en` without scanning secondary languages. On macOS, read the first `AppleLanguages` entry once per invocation with fixed `/usr/bin/defaults` arguments and no shell, authoritative over conflicting POSIX environment/Intl; on other platforms use `LC_ALL → LC_MESSAGES → LANGUAGE → LANG → Intl`. Remove the legacy module-level macOS probe/cache and prohibit watchers or command-time re-detection.
- [x] 2.5 Enforce bootstrap order: capture all source values once → validate all explicit sources → resolve UI/prompt locales → statically compose and validate owner bundles/placeholders once → create one strict translator → create one immutable presentation context → expose commands/render Ink. Discard source readers after composition and prove command, React and Agent runtime paths do not re-read environment/config or detect locale. Inject that same object explicitly into the router and through a package-private transport-only React Context into Ink; keep direct user-config edits restart-only and omit locale state/setters/detection/registry from React plus all Locale management commands, mutation DTOs and file watchers.
- [x] 2.6 Keep Locale preferences outside merged `UnifiedConfig`; define canonical invocation/runtime `promptLocale: SupportedLocale`, resolve `auto` before runtime construction and map it once into any unambiguous prompt-domain-local `locale` field. Migrate touched built-in prompt/Skill/capability/tool-description/validation/compression/AgentSession paths away from optional free-form strings, legacy `zh`/`zh-CN`, local normalization and `locale ?? 'en'`.
- [x] 2.7 Propagate one concrete `promptLocale` through SubAgents, task continuations and invocation-owned background Agent workflows; make resume use the new invocation value without persisting active Locale or rewriting history, and keep custom Skill/workspace/user/history/external text outside automatic translation and built-in bundle parity.

## 3. Presentation and Diagnostic Boundaries

- [x] 3.1 Change affected host ports, hooks, command-family handlers and shared slash handlers to return small typed semantics or diagnostics rather than final success/error sentences; keep the existing outer `TuiCommandRouterResult.output/error` only as Presenter-generated terminal projection; remove legacy successful prose paths and status fields such as `mediaModelSummary`, `perceptionModelSummary`, `llmParameterSummary`, `activeSkillSummary` and `runningTaskSummary` within the migration boundary.
  - Verified: command/status/session paths now return semantic results or typed diagnostics and project them through Presenters. Continuation formatters and all scoped preformatted status fields (`mediaModelSummary`, `perceptionModelSummary`, `llmParameterSummary`, `activeSkillSummary`, `runningTaskSummary`) have been removed; focused residual search returns no matches.
- [x] 3.2 Implement statically imported command-family Presenters using complete atomic owned messages, stable semantic placeholders and explicit semantic rows. Presenters own field grouping, row order, language punctuation and natural-language spacing; terminal adapters own structural glyphs, physical wrapping, continuation indentation, truncation and width behavior. Use direct exhaustive dispatch and only small proven-reused CLI layout helpers; do not add fragment concatenation, a global CJK post-processor, runtime Presenter/formatter registries, locale-specific Presenter copies, string-key lookup, fallback Presenter, global all-command result union, universal command/document AST or generic Menu AST.
- [x] 3.3 Implement stable diagnostic code + localized message + unchanged external detail projection into the outer terminal result, including bounded terminal-safe raw Locale literals and existing machine failure envelopes. Keep programming errors, impossible states and missing Presenter wiring fail-visible rather than converting them into user diagnostics.
- [x] 3.4 Add small explicit locale-aware formatters for human counts, date/time, duration and byte size. Capture the system time zone once during invocation composition; use explicit `Intl.DateTimeFormat` options, explicit duration semantics, fixed binary units `B`/`KiB`/`MiB`/`GiB`, and centralized rounding/precision. Prove IDs, ports, versions, serialized values and unit tokens remain unchanged; do not add user time-zone/unit/precision configuration or re-read host time-zone state during rendering.
- [x] 3.5 Add owner message keys for Commander command/argument/option descriptions, usage wrappers and examples, plus a narrow Commander parse-failure adapter that produces typed terminal diagnostics without forking Commander or adding a general CLI framework.
  - Verified: Commander metadata and parse diagnostics use owner keys, `configureLocalizedCommander()` uses Commander 12's supported `.addHelpCommand()` API, `build:exe` succeeds, and the built Bun executable renders both `zh-cn` and `en` help without startup failure.
- [x] 3.6 Enforce the human channel contract (success/output to stdout, expected diagnostics to stderr, programming/bootstrap/contract failures to stderr plus non-zero exit) and keep bundles plain-text-only without fixed-width padding. Apply ANSI/Chalk/Ink styling, physical wrapping, continuation indentation, truncation and renderer-specific line composition only in terminal adapters while preserving Presenter semantic fields and order across Ink/non-Ink output.
  - Verified: process-level Presenters and CLI action tests cover stdout success, stderr expected diagnostics and non-zero programming/bootstrap failure routing; machine/protocol stdout remains frame-only, and styling/wrapping remains outside plain-text bundles.

## 4. Generic Status Configuration Discovery

- [x] 4.1 Keep `useSlashCommands` as the composition adapter that captures each owning Zustand store exactly once per `/status` request and directly produces one readonly semantic human-status snapshot from those captured references. Audit and reuse canonical exported execution, model-selection, usage, Skill and task read models; where a reusable type is missing, define it at the owning domain/runtime boundary rather than creating a `cli-tui` parallel model. Resolve `getUserConfigPath()` exactly once during terminal composition and inject the captured absolute value as required `userConfigPath` data. Do not add cross-store transactions, locks, versions, subscriptions, caches, a Snapshot Builder, Status Service, repository or synchronization layer; extract a pure projector only after a second real adapter proves duplicate mapping.
- [x] 4.2 Make the package-private status Presenter a pure function of readonly semantic snapshot plus immutable presentation context and the sole owner of row order, punctuation, line breaks, localized labels/state words and count/date/duration/byte formatting. Keep it free of Zustand stores, configuration readers, path APIs and filesystem dependencies; make the router delegate rendering without reading stores or composing status prose; use TypeScript readonly contracts without `Object.freeze`, deep clone, runtime mutation proxies or an immutable-collection framework; preserve stable IDs/external detail and the exact config path without `~`, relative conversion, normalization or duplicate variants.
- [x] 4.3 Test canonical type composition, exactly-once reads for each owning store per snapshot (including a getter poisoned on a second read), best-effort cross-store semantics without synchronization infrastructure, composition-adapter-only store access, readonly construction without runtime freezing, pure Presenter dependencies, router delegation, absence of preformatted owned summary prose and parallel TUI domain DTOs, Presenter-owned status wording/layout, required-field fail-visible behavior, optional semantic absence without empty/`unknown`/`N/A`/English fallback, required path injection, fail-visible missing wiring, stable repeated `/status` output, new-composition re-resolution, missing config files without I/O, and unchanged JSON/debug/evaluation schemas.

## 5. Command Migration

- [x] 5.1 After shared translation, invocation composition and owner bundles are established, migrate `/model`, media and perception list/status/select/reset/menu/usage/error paths to command-family semantic results and statically dispatched Presenters for both `en` and `zh-cn`. Side-effect handlers must return actual canonical post-operation state; menu Presenters produce localized title/label/description over stable IDs, and handlers never parse localized labels.
- [x] 5.2 Add Ink and non-Ink acceptance tests for the first slice, including identical semantic inputs across locales, current/default markers, stable menu identities, actual post-operation state, provider failure wrapping, missing canonical Presenter wiring and poisoned legacy formatters/ports/string adapters.
- [x] 5.3 Migrate remaining mode, parameter, status, context, compact, queue, task, Skill, tool, config, history, MCP, capability, artifact, market, lifecycle and unknown-command output.
  - Verified: all listed command families, continuation/session messages, Tool Approval Ink output, startup/status chrome and unknown-command paths use canonical bundles and Presenters; stale English-path assertions were replaced with locale-aware canonical-path tests.
- [x] 5.4 Migrate each remaining family as one unit containing semantic handler result, Presenter, bundles, tests and same-unit deletion or poisoning of obsolete prose/formatter/key paths. Migrate startup/status chrome, suggestions, terminal labels, one-shot human output and shared Agent/slash handler presentation; prohibit wrapping legacy English, string detection/translation, prose-to-key maps and new/legacy dual success paths. Remove package-local `TuiLocale = 'en' | 'zh'`, `detectTuiLocale`, `getTuiLabels`, `formatTuiTemplate`, `formatTuiLabel` and `TUI_LABELS` together with `NEKO_LOCALE` and the legacy module-level macOS locale probe/cache; retain only the canonical invocation-scoped system-locale adapter and use shared `SupportedLocale` without a `zh` alias, compatibility wrapper or dual runtime.
  - Verified: obsolete locale helpers, `NEKO_LOCALE`, legacy `zh` alias, module-level locale cache, continuation prose formatters, Tool Approval literals and stale unknown-command assertions are removed or replaced in the same migration units. A shared presentation-provider test helper keeps legacy Ink suites on the canonical context.
- [x] 5.5 Keep First Slice Ready separate from Capability Complete until the entire owned-output inventory is closed. Do not add terminal-localization feature flags, staged/percentage rollout, experiment cohorts, kill switches, legacy locale modes, fallback-to-old-English behavior or localization telemetry/analytics; canonical bootstrap fails visibly on invalid contracts, while existing local logging may record an explicit diagnostic.

## 6. Quality Gates and Documentation

- [x] 6.1 Add cross-locale tests proving command syntax, aliases, arguments, IDs, diagnostic codes and machine schemas remain stable; prove external provider/tool details remain unchanged under localized Neko-owned wrappers without introducing a contribution localization adapter. Prove JSON/debug JSONL/structured-log/evaluation stdout contains no human prose, debug protocol stdout is frame-exclusive with structured expected failures, completion scripts are byte-stable, and experiment JSON/Markdown report contracts remain unchanged while only terminal progress/summary is localized.
  - Verified together: CLI action/program, experiment report, debug protocol/stdio and process-presentation suites cover stable command identities and external values, byte-identical completion scripts, frame-exclusive JSONL stdout with structured failures, and locale-neutral experiment JSON/Markdown report content. `pnpm test:agent:eval` additionally validates the key-free evaluation protocol/output contract; it is not claimed as real Agent behavior acceptance.
- [x] 6.2 Add strict-translator tests for missing selected-locale keys, unresolved-key rejection, namespace collisions, key/placeholder parity, invalid/non-named placeholders, literal-brace escaping, missing/extra/undefined parameters, single-pass value insertion, once-per-invocation validation, static owner imports and removed-key poisoning; add human stdout/stderr/exit routing, protocol-exclusivity, Commander parse-diagnostic, complete-message, explicit plural-variant, plain-text bundle, semantic-row/order and deterministic formatter tests with fixed locale/time-zone/input. Keep only manually reviewed full-output goldens for `/status`, Commander help and the first command family; test Ink width/styling at the adapter layer and do not snapshot every message. Prove no invocation calls `setLocale()`, router and Ink receive the same context, `auto` is eliminated before runtime construction, all built-in prompt-bearing paths and child Agents receive one concrete value, resume does not rewrite history, custom/external text is unchanged, old detector/`tui-locale`/legacy `zh` fallback paths are removed or poisoned, and existing Webview `I18nService` behavior remains unchanged. Run focused existing `cli-tui` (`@neko/cli`), `@neko/agent`, shared configuration/i18n tests, `pnpm check`, and relevant legacy-debt/unused checks; do not weaken strict TypeScript or add fallback branches. Use focused residual searches and inventory review rather than adding a dedicated AST checker.
  - Current verified state: strict translator, owner-bundle parity, locale bootstrap, Presenter, router/Ink context and removed-path poisoning pass. Residual cleanup now also covers timeline/backfill wrappers, typed work-directory/config/Locale bootstrap diagnostics, single translator creation per invocation, clean bootstrap stderr, and typed canonical-resume diagnostics localized only at CLI/TUI presentation boundaries. The localization-owned `@neko/cli` suites pass, including presentation (26 files / 130 tests), core routing (27 files / 195 tests), interactive surfaces (21 files / 87 tests), process/bootstrap (7 files / 103 tests), and invocation-local runtime presentation (2 focused tests). A current full-suite rerun reports 84 files / 525 tests passed and one failure in the parallel perception/backfill media-result URL expectation; that unstaged path is outside this change. The runtime presentation tests include an identity assertion that one invocation-scoped presentation object reaches both the Ink Provider and TUI router; focused residual search confirms terminal composition never calls mutable `setLocale()`. The rebuilt Bun executable proves explicit `zh-cn`/`en`, system-locale `auto`, localized invalid-Locale stderr, localized missing-workdir stderr and non-zero exits. The final residual pass also routes experiment failure stderr through a localized Neko-owned wrapper while preserving external error detail and report-domain output unchanged. Repository `pnpm check`, TypeScript baseline, legacy-debt and unused checks were executed and remain red only on the recorded repository-wide debt unrelated to this localization path; they are classified as external baseline rather than duplicated into this change.
- [x] 6.3 Run focused `@neko/shared/i18n`, `@neko/agent` and `@neko/cli` tests, then impact-selected repository type/test/legacy-debt/unused gates. Use `neko-agent-evaluation` only when implementation changes prompt-locale propagation, built-in prompt/Skill/capability/tool-description, AgentSession/SubAgent, validation/recovery or TUI event projection; do not describe key-free harness self-tests as real Agent behavior evidence or add localization-specific CI. Run `neko-quality-review` and record commands, evidence and residual risks.
  - Current verified state: localization-owned `@neko/cli` suites pass (presentation 26/130, core 27/195, interactive 21/87, process/bootstrap 7/103, runtime presentation 2 focused tests), and the previously recorded CLI build/Bun executable plus focused shared i18n and Agent command/handler suites pass. The current full CLI rerun is 84 files / 525 tests passed with one failure caused by the unstaged parallel perception/backfill expectation for generated media result URLs; no localization-owned assertion failed. CLI `tsc --noEmit` still reports 56 existing strict-type errors. None points to the production localization/bootstrap/Presenter files changed by the residual cleanup; four errors remain in three pre-existing `useAgentSession` test files that are also modified elsewhere in the broader working tree. `pnpm check`/`check:unused` fail on current repository Knip debt (1 unused file, 5 unused dependencies, 2 unlisted dependencies, 25 unused exports and 2 duplicate exports), `check:deps` reports two `neko-content` cycles, and `check:legacy-debt` reports 65 `migrate-now` occurrences across 16 files. Focused residual search finds no `setLocale()`, `detectTuiLocale`, `getTuiLabels`, `formatTuiTemplate`, `formatTuiLabel`, `TUI_LABELS`, `NEKO_LOCALE` or `tui-locale` import in terminal composition. Per the user's explicit instruction for this batch, real provider/model Agent evaluation is waived and was not run; it is not a completion blocker for this change, and no key-free harness result is claimed as real behavior evidence.
- [x] 6.4 Update only existing affected Agent/CLI package, domain architecture, README or paired-language documentation whose stable semantics changed; do not add separate decision records, migration logs, command-output diaries or timelines. Mark Capability Complete and archive using this single checklist only when every task/inventory family is closed, bundle/key/placeholder and Presenter/machine-channel contracts pass, touched prompt paths share one concrete locale, obsolete detectors/aliases/environment entrypoints/formatters/keys/dual paths are removed or poisoned, all applicable change-scoped gates pass, impact-selected repository gates are executed with unrelated pre-existing failures explicitly classified, required evaluation is completed or explicitly waived, and verification evidence plus residual risk are recorded; do not add readiness services, runtime status, maturity scores or percentage-completion APIs.
  - Current verified state: this existing checklist is the only implementation/verification inventory; no ADR, rollout service or parallel status document was added. Stable documentation semantics are synchronized here. Real provider/model evaluation is explicitly waived for this batch. Capability Complete is satisfied for this change: all change-scoped contracts and focused gates pass, repository-wide TypeScript, Knip, dependency-cycle and legacy-debt failures are recorded as unrelated pre-existing baselines, and no parallel readiness or debt document was created.
