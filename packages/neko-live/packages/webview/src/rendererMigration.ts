/**
 * Duplicated single-avatar renderers stay enabled until neko-puppet and
 * neko-model Live Mode workflows cover the migration path end to end.
 */
export const NEKO_LIVE_LOCAL_PREVIEW_ENABLED = true;
