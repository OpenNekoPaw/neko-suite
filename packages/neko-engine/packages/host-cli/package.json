{
  "name": "@neko-engine/host-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Command-line tool for neko media processing",
  "scripts": {
    "build:native": "FFMPEG_DIR=\"$(cd ../.. && pwd)/deps/ffmpeg\" cargo build --release",
    "build:debug": "FFMPEG_DIR=\"$(cd ../.. && pwd)/deps/ffmpeg\" cargo build"
  },
  "engines": {
    "node": ">= 18"
  },
  "license": "LGPL-3.0-or-later"
}
