[package]
name = "neko-engine-puppet-renderer"
version.workspace = true
edition.workspace = true
description = "Puppet renderer companion crate for neko-engine"
license.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
neko-engine-gpu = { path = "../engine-gpu" }
neko-engine-types = { path = "../engine-types" }
neko-runtime-puppet = { path = "../runtime-puppet" }

bytemuck.workspace = true
wgpu.workspace = true

[features]
default = []
