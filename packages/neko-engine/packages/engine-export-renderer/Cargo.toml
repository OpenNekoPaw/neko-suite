[package]
name = "neko-engine-export-renderer"
version.workspace = true
edition.workspace = true
description = "Export render pipeline companion support for neko-engine"
license.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
neko-engine-gpu = { path = "../engine-gpu" }

wgpu.workspace = true

[features]
default = []
