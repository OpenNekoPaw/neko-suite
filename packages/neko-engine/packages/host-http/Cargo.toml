[package]
name = "neko-host-http"
version.workspace = true
edition.workspace = true
description = "HTTP/WebSocket server for Neko Engine - RESTful API and streaming"
license.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
# Engine API
neko-host-api = { path = "../host-api" }
neko-engine-types = { path = "../engine-types" }
neko-runtime-scene = { path = "../runtime-scene" }

# HTTP framework
axum = { workspace = true, features = ["ws"] }
tower-http = { workspace = true, features = ["cors"] }

# Async runtime
tokio.workspace = true
tokio-util.workspace = true

# UUID generation
uuid.workspace = true

# ZIP archive reading (for EPUB on-demand entry serving)
zip.workspace = true

# Image metadata probing and preview variant generation
image = { workspace = true, features = ["hdr"] }

# Serialization
serde.workspace = true
serde_json.workspace = true

# Logging
tracing.workspace = true

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "sync", "macros"] }
tempfile = "3"
