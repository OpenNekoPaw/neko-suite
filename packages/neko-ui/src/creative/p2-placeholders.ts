export interface AssetBrowserPlaceholderProps {
  readonly reason: 'deferred-to-asset-federation-adapter';
  readonly targetPhase: 'P2';
}

export interface MediaTransportControlsPlaceholderProps {
  readonly reason: 'deferred-to-package-transport-adapter';
  readonly targetPhase: 'P2';
}
