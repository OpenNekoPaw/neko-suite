/** Canonical source profile for the media-production workflow that adapts comic media. */
export const MEDIA_PRODUCTION_FROM_COMIC_PROFILE_ID = 'media-production/from-comic' as const;

/** Canonical internal artifact profile for an animation production plan. */
export const MEDIA_PRODUCTION_ANIMATION_PLAN_PROFILE_ID =
  'media-production.animation-plan' as const;

/** Canonical internal artifact profile for per-shot image preparation plans. */
export const MEDIA_PRODUCTION_SHOT_IMAGE_PREP_PROFILE_ID =
  'media-production.shot-image-prep' as const;

/** Canonical review artifact profile for shot image preparation plans. */
export const MEDIA_PRODUCTION_SHOT_IMAGE_PREP_REVIEW_PROFILE_ID =
  'media-production.shot-image-prep-review' as const;
