{
  "name": "@neko/agent-cli",
  "version": "0.0.1",
  "description": "CLI for UniEdit AI Agent - standalone agent execution from command line",
  "type": "module",
  "bin": {
    "nekoagent": "./dist/cli.js"
  },
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --dts --clean --minify --target node18 --out-dir dist",
    "build:bundle": "esbuild src/cli.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/cli.bundle.cjs --minify --external:fsevents",
    "build:exe": "npm run build:bundle && pkg dist/cli.bundle.cjs --targets node20-macos-arm64,node20-macos-x64,node20-linux-x64,node20-win-x64 --output ../../nekoagent --compress GZip",
    "build:exe:current": "npm run build:bundle && pkg dist/cli.bundle.cjs --targets node20-macos-arm64 --output ../../nekoagent --compress GZip",
    "dev": "tsx src/cli.ts",
    "test": "vitest",
    "test:run": "vitest --run"
  },
  "pkg": {
    "assets": [],
    "outputPath": "dist"
  },
  "dependencies": {
    "@neko/agent": "workspace:*",
    "@neko/shared": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "inquirer": "^9.2.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.0",
    "@yao-pkg/pkg": "^5.11.0",
    "esbuild": "^0.20.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
