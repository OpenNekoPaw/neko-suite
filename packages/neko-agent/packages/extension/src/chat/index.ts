/**
 * AI Assistant Module (Chat UI Layer)
 *
 * Only ChatViewProvider is the public API — all other classes
 * (SettingsManager, ProviderManager, etc.) are internal to this module.
 */

export { ChatViewProvider } from './chatProvider';
export type { ChatViewProviderOptions } from './chatProvider';
