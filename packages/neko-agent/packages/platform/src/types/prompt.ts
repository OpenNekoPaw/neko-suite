/**
 * Prompt Types - Re-export from shared
 *
 * @deprecated Import directly from @uniedit/shared instead
 */

export type {
  Prompt,
  PromptVariable,
  PromptCategory,
  RenderedPrompt,
  ChainPrompt,
  ChainPromptStep,
  IPromptManager,
} from '@uniedit/shared';
