/**
 * Prompt Types - Re-export from shared
 *
 * @deprecated Import directly from @neko/shared instead
 */

export type {
  Prompt,
  PromptVariable,
  PromptCategory,
  RenderedPrompt,
  ChainPrompt,
  ChainPromptStep,
  IPromptManager,
} from '@neko/shared';
