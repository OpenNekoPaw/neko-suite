/**
 * Tool Types - Re-export from @uniedit/shared
 *
 * DEPRECATED: Import from '@uniedit/shared' directly for new code.
 */

export type {
  // Tool definition
  Tool,
  ToolCategory,
  ToolResult,
  ToolCallRequest,

  // Registry interface
  IToolRegistry,

  // Execution config
  ToolExecutionConfig,

  // Filter options
  ToolFilterOptions,
} from '@uniedit/shared';
