/**
 * Tool Types - Re-export from @neko/shared
 *
 * DEPRECATED: Import from '@neko/shared' directly for new code.
 */

export type {
  // Tool definition
  Tool,
  ToolCategory,
  ToolResult,
  ToolCallRequest,

  // Registry interface
  IToolRegistry,

  // Execution config
  ToolExecutionConfig,

  // Filter options
  ToolFilterOptions,
} from '@neko/shared';
