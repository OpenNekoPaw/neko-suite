{
  "groups": [
    {
      "id": "default",
      "name": "Default",
      "description": "Default model group for general tasks",
      "models": ["openai-gpt-4o", "anthropic-claude-3-5-sonnet", "google-gemini-2-flash"],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": true,
        "maxAttempts": 3,
        "triggerOn": ["rate_limit", "timeout", "server_error", "unavailable"]
      },
      "enabled": true
    },
    {
      "id": "fast",
      "name": "Fast",
      "description": "Optimized for speed and cost",
      "models": ["openai-gpt-4o-mini", "anthropic-claude-3-5-haiku", "google-gemini-1-5-flash"],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": true,
        "maxAttempts": 3,
        "triggerOn": ["rate_limit", "timeout", "server_error", "unavailable"]
      },
      "enabled": true
    },
    {
      "id": "vision",
      "name": "Vision",
      "description": "Models with vision capabilities",
      "models": ["openai-gpt-4o", "anthropic-claude-3-5-sonnet", "google-gemini-1-5-pro"],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": true,
        "maxAttempts": 2,
        "triggerOn": ["rate_limit", "timeout", "server_error"]
      },
      "enabled": true
    },
    {
      "id": "embedding",
      "name": "Embedding",
      "description": "Text embedding models",
      "models": ["openai-text-embedding-3-small", "openai-text-embedding-3-large"],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": true,
        "maxAttempts": 2,
        "triggerOn": ["rate_limit", "timeout", "server_error"]
      },
      "enabled": true
    },
    {
      "id": "image",
      "name": "Image Generation",
      "description": "Image generation models",
      "models": ["openai-dall-e-3"],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": false,
        "maxAttempts": 1,
        "triggerOn": []
      },
      "enabled": true
    },
    {
      "id": "local",
      "name": "Local",
      "description": "Local models via Ollama",
      "models": [],
      "strategy": { "type": "priority" },
      "fallback": {
        "enabled": false,
        "maxAttempts": 1,
        "triggerOn": []
      },
      "enabled": true
    }
  ]
}
