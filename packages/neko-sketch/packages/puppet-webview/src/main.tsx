import React from 'react';
import ReactDOM from 'react-dom/client';
import { PuppetApp } from './PuppetApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PuppetApp />
  </React.StrictMode>,
);
